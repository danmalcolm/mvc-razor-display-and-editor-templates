@*

Layout template shared by all display templates (except DisplaySection.cs). 

This can be used to add any application-specific surrounding HTML. 

Note: DisplaySection.cshtml and DisplayExtensions methods provide a mechanism 
for displaying values with surrounding HTML and label

*@
@RenderBody()